/**
 * w = Width
 * h = Height
 * x = xPos
 * y = yPos
 * rx = Corner Radius X
 * ry = Corner Radius Y
 */
export interface Rectangle {
  w: number;
  h: number;
  x: number;
  y: number;
  rx: number;
  ry: number;
}